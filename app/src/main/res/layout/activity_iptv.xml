<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.media3.ui.PlayerView
        android:id="@+id/iptv_player"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        app:resize_mode="fill" />

    <ProgressBar
        android:id="@+id/iptv_loading"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerInParent="true"
        android:indeterminateTint="@color/neon_red"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/iptv_top_bar"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_alignParentTop="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="#00000000">

        <TextView
            android:id="@+id/iptv_back_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="← Back"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:padding="8dp"
            android:focusable="true"
            android:background="@drawable/nav_item_focus_bg" />

        <TextView
            android:id="@+id/iptv_current_channel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Select a channel"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:paddingStart="16dp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/iptv_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="12sp" />

    </LinearLayout>

    <!-- EPG GUIDE SIDEBAR — fixed width overlay, video visible on right -->
    <LinearLayout
        android:id="@+id/iptv_sidebar"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_below="@id/iptv_top_bar"
        android:orientation="vertical"
        android:background="#990D0D0D"
        android:elevation="8dp">

        <!-- Sidebar header: search on top, context label below -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#00000000"
            android:paddingBottom="4dp">

            <!-- Search — always at top, always accessible -->
            <EditText
                android:id="@+id/iptv_search"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginTop="8dp"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:hint="🔍 Search channels..."
                android:textColor="#FFFFFF"
                android:textColorHint="#555555"
                android:textSize="12sp"
                android:background="@drawable/nav_item_focus_bg"
                android:maxLines="1"
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:nextFocusDown="@id/iptv_group_list" />

            <!-- Context label: Categories or selected category name -->
            <TextView
                android:id="@+id/iptv_category_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="» Categories"
                android:textColor="#E50914"
                android:textSize="11sp"
                android:textStyle="bold"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="8dp"
                android:paddingBottom="4dp" />

            <!-- Hidden stubs so Java findViewByIds do not NPE -->
            <TextView android:id="@+id/iptv_guide_back_btn"
                android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
            <TextView android:id="@+id/epg_date_label"
                android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />

        </LinearLayout>

        <!-- Body: single column — switches between categories and channels view -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- Stage 1: Categories list — full width, visible by default -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/iptv_group_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#00000000"
                android:scrollbars="none"
                android:paddingTop="4dp"
                android:focusable="true"
                android:nextFocusUp="@id/iptv_search"
                android:visibility="visible" />

            <!-- Stage 2: Channels list — full width, shown after category selected -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/iptv_recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#00000000"
                android:scrollbars="none"
                android:clipToPadding="false"
                android:focusable="true"
                android:nextFocusUp="@id/iptv_search"
                android:visibility="gone" />

        </FrameLayout>

    </LinearLayout>


    <!-- Now-playing info card - floats top-right on player, auto-hides after 10s -->
    <LinearLayout
        android:id="@+id/iptv_pip_container"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="56dp"
        android:layout_marginEnd="12dp"
        android:orientation="vertical"
        android:background="#990D0D0D"
        android:padding="10dp"
        android:elevation="10dp"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/pip_logo"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:scaleType="centerInside"
                android:background="#1AFFFFFF"
                android:contentDescription="@string/poster_desc"/>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginStart="8dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <TextView
                        android:id="@+id/pip_channel_name"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:textColor="#FFFFFF"
                        android:textSize="12sp"
                        android:textStyle="bold"
                        android:singleLine="true"
                        android:ellipsize="end"/>

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="LIVE"
                        android:textColor="#FFFFFF"
                        android:textSize="8sp"
                        android:background="#E50914"
                        android:paddingStart="4dp"
                        android:paddingEnd="4dp"
                        android:paddingTop="1dp"
                        android:paddingBottom="1dp"
                        android:layout_marginStart="4dp"/>

                </LinearLayout>

                <TextView
                    android:id="@+id/pip_epg_now"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="#AAAAAA"
                    android:textSize="10sp"
                    android:singleLine="true"
                    android:ellipsize="end"
                    android:layout_marginTop="2dp"/>

            </LinearLayout>
        </LinearLayout>

        <ProgressBar
            android:id="@+id/pip_epg_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:layout_marginTop="6dp"
            android:progressTint="#E50914"
            android:max="100"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/pip_epg_next"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#666666"
            android:textSize="9sp"
            android:singleLine="true"
            android:ellipsize="end"
            android:layout_marginTop="3dp"
            android:visibility="gone"/>

    </LinearLayout>

</RelativeLayout>
